{
  "operationName": "getCustomerById",
  "httpMethod": "GET",
  "path": "/customers/{id}",
  "description": "ID ile müşteri getirir.",
  "requestSchema": {
    "type": "object",
    "pathParameters": {
      "id": {
        "type": "long",
        "required": true,
        "minimum": 1
      }
    }
  },
  "responseSchema": {
    "type": "object",
    "fields": {
      "id": {
        "type": "long"
      },
      "firstName": {
        "type": "string"
      },
      "lastName": {
        "type": "string"
      },
      "email": {
        "type": "string"
      }
    }
  },
  "validationRules": [
    "id 1 veya daha büyük olmalı."
  ],
  "expectedStatusCodes": [
    {
      "status": 200,
      "name": "OK",
      "description": "Müşteri bulundu."
    },
    {
      "status": 400,
      "name": "BAD_REQUEST",
      "description": "ID geçersiz."
    },
    {
      "status": 404,
      "name": "NOT_FOUND",
      "description": "Müşteri bulunamadı."
    }
  ],
  "businessRules": [
    "Mevcut memory tabanlı CustomerService yapısını kullan.",
    "Yeni müşteri storage/service implementasyonu oluşturma.",
    "ID ile kayıt bulunabiliyorsa mevcut CustomerResponse formatında döndür.",
    "Kayıt yoksa 404 döndür."
  ],
  "persistenceRequired": false,
  "externalServiceRequired": false,
  "acceptanceCriteria": [
    "GET /customers/{id} mevcut müşteri için 200 dönmeli.",
    "Response mevcut CustomerResponse kontratını kullanmalı.",
    "Olmayan müşteri için 404 dönmeli.",
    "Geçersiz id için 400 dönmeli.",
    "Mevcut POST /customers davranışı bozulmamalı.",
    "Mevcut GET /hello davranışı bozulmamalı.",
    "Endpoint için test yazılmalı.",
    "./mvnw test başarılı olmalı.",
    "./mvnw clean package başarılı olmalı."
  ]
}
